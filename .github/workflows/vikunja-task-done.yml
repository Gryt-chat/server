name: Sync Vikunja task

# Keeps the task named in the branch in step with the PR. Needs the org secret
# VIKUNJA_API_TOKEN.

on:
  pull_request:
    types: [opened, reopened, closed]

jobs:
  sync-task:
    uses: Gryt-chat/.github/.github/workflows/vikunja-task-done.yml@main
    secrets: inherit
