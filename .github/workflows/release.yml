name: Release Server

description: |
  Release Gryt server as beta or production. Builds multi-arch Docker image, tags, pushes, and creates a GitHub Release.

on:
  workflow_dispatch:
    inputs:
      channel:
        description: 'Release Channel'
        required: true
        default: 'beta'
        type: choice
        options: ['beta', 'latest']

  push:
    tags:
      - 'v*.*.*'

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    env:
      IMAGE: ghcr.io/gryt-chat/server
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile --ignore-engines

      - name: Type-check and lint
        run: |
          npx tsc -b
          npx eslint src/

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version
        id: version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Determine channel
        id: channel
        run: |
          if [[ "${{ github.event_name }}" == 'workflow_dispatch' ]]; then
            CHANNEL="${{ github.event.inputs.channel }}"
          elif [[ "${{ github.ref }}" == refs/tags/* ]]; then
            CHANNEL="latest"
          else
            CHANNEL="beta"
          fi
          echo "channel=$CHANNEL" >> $GITHUB_OUTPUT

      - name: Docker Build and Push
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          CHANNEL="${{ steps.channel.outputs.channel }}"
          V_MAJOR=$(echo "$VERSION" | cut -d. -f1)
          V_MINOR=$(echo "$VERSION" | cut -d. -f2)

          TAGS=(
            "${IMAGE}:$VERSION"
            "${IMAGE}:$V_MAJOR.$V_MINOR"
            "${IMAGE}:$V_MAJOR"
            "${IMAGE}:latest-beta"
          )
          if [[ "$CHANNEL" == "latest" ]]; then
            TAGS+=("${IMAGE}:latest")
          fi
          ARGS=( )
          for TAG in "${TAGS[@]}"; do
            ARGS+=( -t "$TAG" )
          done

          docker buildx build \
            --platform linux/amd64,linux/arm64 \
            --build-arg VERSION="$VERSION" \
            --cache-from type=registry,ref=${IMAGE}:buildcache \
            --cache-to type=registry,ref=${IMAGE}:buildcache,mode=max \
            "${ARGS[@]}" \
            --push .

      - name: Tag and create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "v${{ steps.version.outputs.version }}"
          prerelease: ${{ steps.channel.outputs.channel == 'beta' }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
